Let's get started! You need to:
  
  * Create a directory for your project.

  ```
  mkdir eth-fun
  cd eth-fun
  npm init
  ```

  * Install "solc" from npm.

  ```
  npm install solc
  ```

  Use `$ADVENTURE_COMMAND verify` to verify your environment.

